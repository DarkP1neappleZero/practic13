"""
Задача 4. Проверка IP-адреса
"""

# Ввод IP-адреса
ip_address = input()

# Разделяем строку на части по точке
parts = ip_address.split('.')

# Проверяем количество частей
if len(parts) != 4:
    print("НЕТ")
else:
    # Флаг для проверки корректности
    valid = True
    
    # Проверяем каждую часть
    for part in parts:
        # Проверка 1: часть не должна быть пустой
        if part == "":
            valid = False
            break
            
        # Проверка 2: все символы должны быть цифрами
        for char in part:
            if char < '0' or char > '9':
                valid = False
                break
        if not valid:
            break
        
        # Проверка 3: число в диапазоне 0-255
        num = int(part)
        if num < 0 or num > 255:
            valid = False
            break
    
    # Выводим результат
    if valid:
        print("ДА")
    else:
        print("НЕТ")
