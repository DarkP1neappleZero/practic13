n = int(input("Введите количество строк: "))
all_symbols = []

for i in range(n):
    line = input(f"Введите строку {i + 1}: ")
    all_symbols.extend(list(line))

print(f"Список всех символов:", all_symbols)
